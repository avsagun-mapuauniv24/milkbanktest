Redesign the sidebar navigation for dark mode. Background #13151F, sidebar width 220px. System name "Makati Milk Bank" in white, subtitle "Inventory System" in muted blue-gray. Navigation items use muted gray text with colored dot indicators. Active item has a teal left border, soft teal glow background highlight (#1D9E7520), and white text. Hover state adds a subtle #2A2D3E background. Bottom user profile area shows avatar circle with initials, name in white, role in muted gray. No harsh borders, everything feels soft and deep. Compare side by side with the light version.
Design a dark mode Dashboard page for the Makati Milk Bank Inventory System. Page background #0F1117, top bar background #1A1D27 with a thin #2A2D3E bottom border. Stat cards use #1E2130 background with colored numbers (teal, blue, amber, coral) and light gray labels. Progress bars use #2A2D3E as the track and vibrant teal/blue/amber fills. Recent Activity table uses #1A1D27 rows with #2A2D3E row dividers and white primary text, muted secondary text. Status badges remain colorful but slightly desaturated. The overall feel should be deep navy, calm, and professional like a medical monitoring system.Design a dark mode Inventory page for the Makati Milk Bank app. Background #0F1117. Search bar and filter dropdown use #1E2130 background with #2A2D3E border and white text. Table header row uses #1A1D27 with muted uppercase labels. Table rows alternate between #0F1117 and #141720. Column text is white for primary info and muted gray for secondary info. Stage badges stay colorful: donated (teal), pasteurized (blue), dispensed (amber). Expiry warning rows add a subtle #D85A3015 red tint background and a small coral warning icon. Action buttons are ghost style with colored icon only. Add New Lot button is solid teal.Design a dark mode Add New Milk Lot modal for the Makati Milk Bank app. Overlay is #00000080 semi-transparent dark. Modal background #1E2130, border #2A2D3E, border radius 12px, width 480px. Title text white, close X button muted gray. Form field labels in light gray, input backgrounds #13151F, input borders #2A2D3E, input text white, placeholder text #5C6480. Focus state adds a teal #1D9E75 border glow. Dropdown arrow in muted teal. Cancel button is ghost outlined in #2A2D3E with gray text. Save button is solid teal #1D9E75 with white text. Feels modern, clean, and easy on the eyes.Design a dark mode Reports page for the Makati Milk Bank app. Background #0F1117. Filter bar inputs and dropdowns use #1E2130 with #2A2D3E borders. Generate Report button solid teal. Chart card background #1E2130 with a subtle #2A2D3E border. Bar chart uses teal, blue, and amber colored bars on a dark #13151F chart background, gridlines in #2A2D3E, axis labels in muted gray, chart title in white. Export PDF button outlined coral, Export Excel button outlined green. Everything feels like a professional medical analytics dashboard at night.Create a side-by-side comparison frame in Figma showing Light Mode vs Dark Mode for the Makati Milk Bank Inventory System. Show these 3 pages in both modes: Dashboard, Inventory, and Reports. Light mode on the left column, dark mode on the right column. Label each column clearly. Use a neutral gray presentation background (#E8E9EC for light context, #080A0F for dark context). Add a small toggle switch component at the top of each pair showing which mode is active. This frame will be used in the capstone presentation slides.Design a dark mode toggle switch component for the Makati Milk Bank top navigation bar. The toggle is a pill-shaped switch, width 52px height 28px. Light mode state: white background, sun icon on the left in amber, circle thumb on the right side in white with a light gray shadow. Dark mode state: deep navy #1A1D27 background, moon icon on the right in soft blue, circle thumb on the left in teal. Add a smooth transition animation between states. Place it in the top right corner of the top bar next to the notification bell and user avatar. Show both states clearly in the design.Update the Figma prototype flow for the Makati Milk Bank Inventory System to include dark mode. Add a dark mode toggle in the top bar. When clicked, all screens switch to their dark mode versions using Figma variable mode switching. Prototype flow: Login (dark) → Dashboard (dark) → toggle to light mode → Dashboard (light) → Inventory → Add Lot Modal → Donors → Donor History panel. Use dissolve transitions between pages and an instant swap when the dark/light toggle is clicked. Make it smooth and impressive for the capstone defense demo.Design a Login page for the Makati Milk Bank Inventory System in both light and dark mode. Centered card layout. Light mode: white card on a soft teal gradient background (#E1F5EE to #B5F0D8). Dark mode: #1E2130 card on a deep navy background (#0A0C14). Both versions include: a teal circular logo icon at the top, system name "Makati Milk Bank Inventory System" in bold, subtitle "Makati Medical Center" in muted color, email input, password input with show/hide toggle, a solid teal Login button full width, and a small "Forgot password?" link below. Clean, trustworthy, hospital-style. Show both modes side by side.
